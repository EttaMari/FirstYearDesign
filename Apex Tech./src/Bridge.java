//finds the bridge and stores variable to relocate bridge
public class Bridge {
	//calls sensors/ motors to move to first postion 
	//calls sonar sensor to check if bridge there
	// if it is call sensor/motor to move acroos
	//if not move to next spot and repeat 
	//set variable for spot used 
}
